<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.nakshi.rohitour.repository.review.ReviewCommentMapper">

    <resultMap id="CommentMap" type="com.nakshi.rohitour.dto.ReviewCommentDto">
        <id     property="id"         column="id"/>
        <result property="reviewId"   column="review_id"/>
        <result property="userId"     column="user_id"/>
        <result property="userName"   column="user_name"/>
        <result property="userRole"   column="user_role"/>
        <result property="content"    column="content"/>
        <result property="createdAt"  column="created_at"/>
        <result property="updatedAt"  column="updated_at"/>
    </resultMap>

    <select id="findByReviewId" resultMap="CommentMap">
        SELECT rc.id, rc.review_id, rc.user_id,
               u.name AS user_name, u.role AS user_role,
               rc.content, rc.created_at, rc.updated_at
        FROM review_comments rc
        JOIN users u ON rc.user_id = u.user_id
        WHERE rc.review_id = #{reviewId}
        ORDER BY rc.created_at ASC
    </select>

    <select id="countByReviewId" resultType="int">
        SELECT COUNT(*) FROM review_comments WHERE review_id = #{reviewId}
    </select>

    <select id="findById" resultMap="CommentMap">
        SELECT rc.id, rc.review_id, rc.user_id,
               u.name AS user_name, u.role AS user_role,
               rc.content, rc.created_at, rc.updated_at
        FROM review_comments rc
        JOIN users u ON rc.user_id = u.user_id
        WHERE rc.id = #{id}
    </select>

    <insert id="insert" parameterType="com.nakshi.rohitour.dto.ReviewCommentDto"
            useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO review_comments (review_id, user_id, content, created_at, updated_at)
        VALUES (#{reviewId}, #{userId}, #{content}, NOW(), NOW())
    </insert>

    <update id="update" parameterType="com.nakshi.rohitour.dto.ReviewCommentDto">
        UPDATE review_comments
        SET content = #{content}, updated_at = NOW()
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM review_comments WHERE id = #{id}
    </delete>

    <delete id="deleteByReviewId">
        DELETE FROM review_comments WHERE review_id = #{reviewId}
    </delete>

</mapper>
